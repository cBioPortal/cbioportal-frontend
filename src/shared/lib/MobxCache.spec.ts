import {assert} from "chai";
import MobxCache from "./MobxCache";

describe("MobxCache", ()=>{
    it("returns the same promise every time get is called with the same query", ()=>{
        const cache = new MobxCache
    });
    it("returns the same promise every time getAll is called with the same queries", ()=>{

    });
});